# Code Sentinel

A security dashboard and incident logging system.

## Usage

1. Create a `CodeSentinel` instance.
2. Add incidents and PRs using the `add_incident` and `add_pr` methods.
3. Get the dashboard data using the `get_dashboard_data` method.

## Testing

Run the tests using `pytest`.
