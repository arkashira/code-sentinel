[tool.poetry]
name = "code-sentinel"
version = "0.1.0"
description = "Code Sentinel for actionable remediation suggestions"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
pytest = "^7.1"
