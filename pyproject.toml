[project]
name = "code-sentinel"
version = "0.1.0"
description = "A tiny CI/CD scanner for GitHub Actions"
requires-python = ">=3.8"
authors = [{name = "Axentx"}]
readme = "README.md"
license = {text = "MIT"}

[tool.setuptools.packages.find]
where = ["src"]
